# Netlify configuration for Next.js deployment
# This file is at the project root to properly configure the frontend deployment

[build]
  base = "frontend"
  command = "npm run build"
  # Publish is relative to base; next build outputs to .next inside frontend/
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# API proxy to backend (Render)
[[redirects]]
  from = "/api/*"
  to = "https://last-piece-4l3u.onrender.com/api/:splat"
  status = 200
  force = true

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Cache static assets
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
